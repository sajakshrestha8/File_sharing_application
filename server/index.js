/**
 * Compatibility entry: run the DDD TypeScript server (compiled to `dist/`).
 *
 * Development: `npm run dev` (ts-node, no build step).
 * Production: `npm run build` then `npm start` or `node dist/main.js`.
 */
require("./dist/main.js");
